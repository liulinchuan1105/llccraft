# 域名账户 · 状态回填

> 最后更新：2026-07-01
> 文件用途：namesilo 后台关键信息 + 后续迁移要点。**别让一年后的自己再回后台翻**。
> **回填完成度**:✅ 已就位（2026-07-01）· 看板 001 闭环

---

## 域名基础信息

| 项 | 值 |
|---|---|
| **域名** | `llccraft.com` |
| **注册商** | namesilo.com |
| **到期日** | 2026-09-25（还有约 86 天） |
| **距离过期** | 🟡 充裕但 **自动续费已关**，需手动续费 |
| **自动续费** | 🔴 **关**（用户决定暂不开，到期前手动续） |
| **WHOIS 隐私** | namesilo 默认免费开 |

---

## 🔴 手动续费提醒（关键时间点）

- **2026-09-25 到期** → **必须在此之前手动续**（过期 = 域名被释放 = 拿不回来）
- **建议提前一周**（即 2026-09-18 前）登 namesilo 点续费
- **续费价格**：.com 约 $9/年
- **续费方式**：信用卡 / PayPal（namesilo 都收）
- ⚠️ 续费操作失误或忘记 = 项目域名蒸发，重买得花更多甚至买不到

> 行动：**用户自己日历/手机提醒设一下**（跨会话可能工具都换，外部提醒最稳）
> 项目内部也记一笔 → 已加进任务跟踪。

---

## DNS 当前状态

| 项 | 值 |
|---|---|
| **DNS 服务商** | ✅ **阿里云 DNS 控制台**（alidns.com，`ns1.alidns.com` / `ns2.alidns.com`）—— 2026-07-01 nslookup 验证 |
| **当前 A 记录** | `47.254.35.99` → 指向 **老阿里云中国 ECS**（已停机） |
| **TTL** | `7207`（用户后台观察到的原值） |
| **当前访问效果** | ❌ 打不开（ECS 关了，DNS 解析到没在跑的服务器） |

**注**：47.254.x.x 是阿里云**中国** ECS 段。和将来要开的**阿里云国际**主机不一个 IP 段。

---

## 🟡 新决策点：阿里云国际主机开起来后，A 记录改哪改？

| 方案 | 怎么做 | 利弊 |
|---|---|---|
| **方案 1：国际版账号 + 迁 DNS 到 namesilo** | namesilo 改 nameservers（指向 namesilo 默认 NS）→ A 记录改在 namesilo 后台 | ✅ 国际账号和 DNS 独立简洁  ❌ 多一次迁移 |
| **方案 2：共用一个阿里云账号（中国 + 国际）** | 用同一个阿里云账号开国际 ECS，DNS 继续在阿里云 DNS 控制台 | ✅ 最省事   ❌ 国际/中国账号混合，财务/合规可能乱 |
| **方案 3：阿里云国际账号 + 国际版 DNS 控制台** | 国际账号里重新配置 DNS 解析 | ✅ 国际独立   ❌ 等于把现有 DNS 记录原样搬一遍 |

**👉 推荐方案 2**（省事为主，起步阶段；起量后再考虑拆账号）
**👉 推荐方案 1**（DNS 管理最干净，但要先在 namesilo 设置 nameservers → 生效需 24-48h）

> 此项决策和"阿里云国际选型"绑定（Task #2），一起拍板。

---

## 🔴 必须做的迁移动作（新主机开起来后立刻做）

迁移顺序 —— **千万别本末倒置**：

1. **先**在阿里云国际开 ECS、拿到新公网 IP（暂记为 `NEW_IP`）
2. **再**改 namesilo 的 DNS A 记录：`llccraft.com` → `NEW_IP`（取代 47.254.35.99）
3. **最后**才把 WP 装进新 ECS + 上线

> 改完 A 记录全球生效最长 48 小时（一般几分钟到几小时）。
> 改记录同时建议把 TTL 临时调到 300 秒，等稳定后再改回 3600/86400。

---

## 风险提示

- 🔴 **过期 = 拿不回来**：到 9 月前若自动续费没成功，必须手动续上
- 🟡 **47.254.35.99 是旧 ECS**：如果哪天 ECS 释放了，这 IP 可能被别人分到（阿里云会回收），DNS 解析到陌生 IP 有**小概率**被人劫持。建议站点关停期间，**要么把 A 记录删掉**，**要么改成 47.254.35.99 占位指向**（= 维持现状不影响）
- 🟢 .com 续费 ~$9/年，便宜

---

## 配合动作清单

- [x] ✅ 后台确认自动续费开关 → 已确认：**关**（用户决定暂不开，到期前手动续）
- [x] ✅ 后台确认 DNS 服务商 → 已确认：**阿里云 DNS**（alidns.com，2026-07-01 nslookup 验证）
- [x] ✅ 域名确认 = `llccraft.com`
- [x] ✅ 到期日 = 2026-09-25
- [x] ✅ 旧 A 记录 = 47.254.35.99（已记录，待迁）

---

## 更新记录

- 2026-07-01　看板任务 001 闭环（清单项已勾、顶层戳记已加、信息一致性确认）
- 2026-07-01　初版骨架 + 用户口头确认信息落档（域名 / 到期日 / 旧 A 记录）
