# 推送公钥运行记录 · 2026-07-01

> 配套文件：`push_key-pywinpty-2026-07-01.py`（同目录）
> 文件用途：审计员 B 挑刺 #1 的回头补存证 —— 让 A / B 都能从这里追溯"C 方案"是怎么执行的、谁跑的、跑出什么结果

---

## 时间线

| 时间 | 动作 | 输出 | 来源 |
|---|---|---|---|
| 2026-07-01 15:21 | 本地生成 ed25519 SSH 密钥 | `~/.ssh/llccraft_sg` + `~/.ssh/llccraft_sg.pub` | Claude Code 本机 |
| 2026-07-01 15:30 | 自动化测试 pip + pexpect + pywinpty | `pexpect 4.9.0` + `pywinpty 3.0.5` 装上 | Claude Code 本机 |
| 2026-07-01 15:45 | 自动化测试 pywinpty + 推 key（用 `your_password_placeholder_REPLACE_ME` 占位）| Permission denied（密码占位） | Claude Code 本机 |
| 2026-07-01 16:XX | pywinpty verbose 探测 → 证实密码注入 OK 但密码值不对 | `read_passphrase: can't open /dev/tty` | Claude Code 本机 |
| 2026-07-01 16:30 | Claude Code 给出 C 方案（**让用户在 PowerShell 跑我写好的脚本**） | 一行 PowerShell 命令 | 聊天框 |
| 2026-07-01 17:XX | **老板在 PowerShell 执行该脚本**（手输真密码）| ✅ KEY PUSHED OK | **用户 PowerShell 终端** |

## 关键事实

- 服务器：`8.219.190.222`（新加坡沙盒 · Ubuntu 22.04.5 LTS · root 用密码认证）
- 本地密钥对：`~/.ssh/llccraft_sg`（ed25519，No passphrase，comment=`llccraft-sg-sandbox-2026-07-01`）
- 公钥指纹：`SHA256:IOFn7rHfQNxWPxF2siYrd8LhDzOd3ksdZwLBPkrcg9s`
- 服务端结果：`/root/.ssh/authorized_keys` 多了一行（echo `wc -l` 输出 ≥ 1）

## 后续验证（执行员 A 在 C 跑完后立刻自测）

- `ssh -i ~/.ssh/llccraft_sg root@8.219.190.222 'whoami'` → `root` ✅
- `ssh -i ~/.ssh/llccraft_sg root@8.219.190.222 'uname -a'` → `Linux iZt4n88trubywnqs8nxshcZ 5.15.0-142-generic` ✅
- 系统：`Ubuntu 22.04.5 LTS (Jammy Jellyfish)` ✅（与计划完全匹配）

## 教训 / 待改进

- 🟡 Windows MSYS2 无 pty/tty 全套：sshpass、expect、setsid 全没有 → 自动化推送 SSH 公钥需要 pywinpty（已装）
- 🟡 下次类似动作，提前准备 pywinpty 一行化脚本,不让老板手动跑 PowerShell
- 🟡 "verify at every step" 的纪律 = 5 个回合调试的本可避免
