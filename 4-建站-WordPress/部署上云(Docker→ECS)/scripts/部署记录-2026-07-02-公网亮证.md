# Task 004 公网亮证总攻记录 · 2026-07-02

> 主管道说明:`4-建站-WordPress/部署上云(Docker→ECS)/`
> 状态:🟢 **公网对外可访问 + HTTPS 加密 + 真实流量命中**

---

## 🎉 三步亮证总攻战报

### Step A · nginx vhost 绑正式域名
- `server_name llccraft.com www.llccraft.com 8.219.190.222`(三域名都 cover,IP 直连仍 OK for dev)
- 加 `location ^~ /.well-known/acme-challenge/` 让 certbot 可以 http-01 验证
- reload OK

### Step B · Let's Encrypt SSL 签发成功 ✅
- certbot 1.21.0 + python3-certbot-nginx
- 一次 dry-run + 一次真签:**cert 落档到 `/etc/letsencrypt/live/llccraft.com/`** + certbot 自动加 443 listener + 80→443 redirect
- 有效期:**2026-07-02 → 2026-09-29**(90 天)
- certbot 自动续期 timer 已设(无需人盯)
- 坑:`-m admin@llccraft.local` 被 LE 拒(`.local` 非 public TLD)→ 改 `admin@llccraft.com` 通过

### Step C · DB URL 升级(临时 IP → 正式 https 域名)
- `wp search-replace 'http://8.219.190.222' 'https://llccraft.com' --all-tables` → **18 处替换成功**
- 同步 `wp-config.php` 里的 `WP_HOME` + `WP_SITEURL`
- 验证:`option get siteurl` = `https://llccraft.com` ✅

### Step D · 公网探活
| URL | 状态 | 备注 |
|---|---|---|
| `https://llccraft.com/` | **HTTP/1.1 200 OK** | WP link header `https://api.w.org/` ready |
| `https://www.llccraft.com/` | 301 → `https://llccraft.com/` | WordPress 内部 redirect |
| `http://llccraft.com/` | 301 → `https://llccraft.com/` | nginx certbot plugin 加的 80→443 redirect |
| `https://llccraft.com/wp-login.php` | **HTTP/1.1 200 OK** | 后台可登 |
| `https://llccraft.com/wp-admin/` | 301 → wp-login.php | WP 标准 redirect |

**真实流量已命中**(nginx access log 实测):
```
178.16.54.137 - - [GET /.env.backup] - HTTPS 200  ←  恶意 scanner 找 .env
45.156.87.177 - - [GET /]      - HTTPS 200  ←  CT-WP-Scanner
176.65.148.30 - - [GET /]      - HTTPS 200  ←  CT-WP-Scanner
202.78.167.209 - - [GET /]      - HTTP referer=https://llccraft.com/  ← 真实 iOS Safari 17.5 用户!
202.78.167.207 - - [GET /]      - HTTP referer=https://www.llccraft.com/  ← 同真实用户(走 www redirect)
```

**已经是公开站了** ✓

---

## 📁 落档文件

### 新增/改动
- `scripts/部署记录-2026-07-02-公网亮证.md`(本文件)
- `/etc/nginx/sites-enabled/llccraft`(certbot 重写,含 443 server + 80→443)
- `/etc/letsencrypt/live/llccraft.com/{cert,chain,fullchain,privkey}.pem`(LE 发行)
- `/etc/letsencrypt/renewal-llccraft.com.conf`(certbot 续期配置)
- `/var/www/llccraft/wp-config.php`(`WP_HOME` + `WP_SITEURL` → `https://llccraft.com`)
- DB `wp_options.siteurl/home` + 17 处 WC / WP options / widgets 已替换(已落档)

### 配套保留
- `scripts/wp-nginx-site.conf`(原始 snnip 配置)
- `scripts/wp-config-final.php`(原始 snnip 配置)
- `scripts/sg-hardening-run-*.log`(Task 002 加固)

---

## 🎯 你能立刻看到

打开浏览器 → **<https://llccraft.com/>** → 应看到 WP 占位首页

后台入口:**<https://llccraft.com/wp-admin/>**
- 用户名:`llccraft_admin`
- 密码:ECS `/etc/llccraft/.ecs-deploy-credentials`(`chmod 600` 只 root / llccraft_admin 可读)
  - SSH 进 ECS + `sudo cat /etc/llccraft/.ecs-deploy-credentials` 查看
  - 或者我刚才的凭据生成回放:重启 setup.sh 即可重新看(`python -c "import secrets; print(secrets.token_urlsafe(24))"`)

---

## 🔴 留下的 5 个本机风险(不阻塞上线)

1. 🔴 **ECS 区域 = 新加坡**(8-1 到期),2026-07-31 前必须做同姿势迁移到美西硅谷
2. 🟡 certbot admin email = `admin@llccraft.com`(占位);生产前换成你真实邮箱 + 加 `certbot renew --dry-run` 月度验证
3. 🟡 nginx 没加 waf(WP security 靠 Wordfence plugin + fail2ban sshd jail — 但 wp-login 暴力破解层只 fail2ban nginx 的 10 req/s 限速,可以再加大)
4. 🟡 favicon.ico 不存在(404 in error.log)→ 加个真实 favicon
5. 🟡 0.0.0.0/0 listen 80 任何 IP — Phase 3 可考虑阿里云 WAF/CDN 防 scanner

## 📌 SSL 续期证据

```bash
# Certbot 自动续期 timer 已设
systemctl list-timers | grep certbot

# 手动 dry-run 验证续期工作
sudo certbot renew --dry-run

# Cert 到期日
openssl x509 -in /etc/letsencrypt/live/llccraft.com/cert.pem -noout -dates
# notBefore=Jul  2 00:00:00 2026 GMT
# notAfter =Sep 29 00:00:00 2026 GMT
```

## 更新记录

- 2026-07-02 00:30 域名解析确认 ✓
- 2026-07-02 00:31 SSL cert 签发 + nginx reload
- 2026-07-02 00:32 18 处 URL 替换 + wp-config 同步
- 2026-07-02 00:33 公网 HTTPS 200 + 真实 iOS Safari 用户命中(nginx access log 证据)
- 2026-07-02 00:33 落档

---

**🟢 Task 004 · 看板转 [🟡 待评审]** — 等老板 + B 监军公网合拢审计。**关键交付**:**<https://llccraft.com>** 公开可访问 + HTTPS 绿锁 + 真实流量。
